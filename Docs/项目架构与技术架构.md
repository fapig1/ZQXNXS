# 桌上有龙：项目架构与技术架构

> 建立日期：2026-09-11。本文把分散在开题说明、开题报告、AGENTS.md 与技术栈文档里的架构描述
> 收拢成一份可施工的说明书，并记录**骨架已搭建到哪一步**。
>
> **状态纪律（2026-09-16 更新）：本文描述的目录、程序集与代码骨架已经写入 `ARPet/` 并已初始化。
> 2026-09-16 在真实 Unity `6000.0.83f1` + Vuforia Engine `11.4.4` 下完成首次验证：
> 11 个程序集编译通过、83 / 83 EditMode 测试通过、场景与配置资产已生成。
> 真机验证尚未执行。** 凡是没做过的，文中一律标"未执行"。
> 本轮修复的 P1 缺陷（`versionDefines` 表达式非法导致 Vuforia 宏从未定义）见
> [代码审查问题与修复记录](../代码审查问题与修复记录.md) 第 4.5 节。

审查与回归证据：[代码审查问题与修复记录](../代码审查问题与修复记录.md)。

相关文档：[技术栈与开发环境](技术栈与开发环境.md)（工具与环境事实）、
[环境缺失与下载清单](环境缺失与下载清单.md)（要下载什么）、
[奶蛙最终版资产说明](奶蛙最终版资产说明.md)（冻结的角色资产）、
[开题报告](开题报告_AR萌宠互动系统.md) 4.1 节（本文的架构来源）。

---

## 1. 一页总览

~~~mermaid
flowchart TB
    subgraph 现实侧
        CAM[手机摄像头]
        DEN[小窝卡<br/>Vuforia ImageTarget]
        FOOD[食物卡<br/>Vuforia ImageTarget]
    end

    subgraph 跟踪层["① 跟踪层 Scripts/AR"]
        VS[VuforiaTrackingSource<br/>或 StubTrackingSource]
        MR[MarkerRegistryBase<br/>小窝局部坐标换算]
        TS[TrackingStateMachine<br/>识别中/有效/丢失/恢复]
    end

    subgraph 交互层["② 交互判定层 Scripts/Interaction"]
        FR[FeedingRule<br/>距离·高度·停留·冷却·再进入]
        TP[TouchPokeRule<br/>触屏逗弄]
        IP[InteractionPipe<br/>总闸门 + 去重]
    end

    subgraph 行为层["③ 行为状态机层 Scripts/Pet"]
        PN[PetNeeds<br/>饥饿·开心·精力]
        BP[BehaviorPolicy<br/>优先级决策]
        BSM[BehaviorStateMachine<br/>唯一结算点]
        CFG[PetConfig<br/>ScriptableObject]
    end

    subgraph 表现层["④ 表现层 Scripts/Presentation"]
        PP[PetPresenter<br/>Animator·表情·音效]
    end

    subgraph 界面层["⑤ 界面层 Scripts/UI"]
        UI[SafeAreaFitter<br/>状态面板·提示·图鉴]
    end

    subgraph 数据层["⑥ 数据层 Scripts/Persistence"]
        SS[SaveService<br/>版本化 JSON + 异常恢复]
        SC[SaveCoordinator<br/>节流写盘 + 离线衰减]
    end

    subgraph 平台层["⑦ 平台层 Scripts/Platform"]
        PL[CameraPermission<br/>AppLifecycle·PhotoCapture]
    end

    APP[AppRoot<br/>唯一组装点]

    CAM --> VS
    DEN --> VS
    FOOD --> VS
    VS --> MR --> FR
    VS --> TS
    TS -->|跟踪不可用则闸门关闭| IP
    FR --> IP
    TP --> IP
    IP --> BSM
    BP --> BSM
    CFG --> BSM
    PN <--> BSM
    BSM --> PP
    BSM --> SC
    SC <--> SS
    PN --> UI
    TS --> UI
    PL --> TS
    APP -.组装.-> VS
    APP -.组装.-> BSM
    APP -.组装.-> IP
~~~

**一句话概括**：跟踪层只回答"卡在哪、可不可信"，交互层只回答"这算不算一次操作"，
行为层只回答"接不接受、现在干什么、什么时候结算"，其余三层只负责"怎么显示、怎么存、怎么适配系统"。

---

## 2. 七层架构（自下而上）

开题报告定义的是五层：跟踪 → 交互 → 行为 → 表现 → 数据。施工时把**界面**与**平台适配**拆成独立程序集，
理由是这两块的改动频率与失败模式跟另外五层完全不同（界面随设计稿变、平台随安卓版本变），
但它们**不是新的业务层**，仍归入表现与数据两条纵向切面。层数不同不改变原有分层语义。

| # | 层 | 命名空间 | 程序集 | 职责 | 明确不做 |
| --- | --- | --- | --- | --- | --- |
| ① | 跟踪层 | `ZQXNXS.ARPet.AR` | `ZQXNXS.ARPet.AR` | 目标标识、位姿、有效性、更新时刻、小窝局部坐标换算 | 不修改任何游戏数值；不做交互判定 |
| ② | 交互判定层 | `...Interaction` | `...Interaction` | 把空间关系与触屏输入转成**一次性事件** | 不决定宠物接不接受；不碰动画 |
| ③ | 行为状态机层 | `...Pet` | `...Pet` | 长期状态数值、行为优先级、打断规则、**唯一结算点** | 不读位姿；不做距离判定 |
| ④ | 表现层 | `...Presentation` | `...Presentation` | Animator、表情、音效、特效 | 不写任何业务规则 |
| ⑤ | 界面层 | `...UI` | `...UI` | 状态面板、跟踪提示、反应图鉴、安全区适配 | 输入不得穿透到角色 |
| ⑥ | 数据层 | `...Persistence` | `...Persistence` | 版本化本地 JSON、异常恢复、写盘节奏 | 不保存场景对象引用 |
| ⑦ | 平台层 | `...Platform` | `...Platform` | 摄像头权限、生命周期、相册 | 不做业务判断 |

装配层 `Scripts/App/AppRoot.cs`（程序集 `ZQXNXS.ARPet.App`）不属于上面任何一层：
它是**唯一**知道各层如何拼在一起的地方，因此业务层可以完全脱离场景运行。

### 依赖方向由程序集强制

~~~text
Platform ─┐
UI ───────┼─→ Core
Presentation ─┤
Persistence ──┼─→ Pet ─→ Core
Interaction ──┘        ↑
AR ────────────────────┘（AR 只依赖 Core）
App ──→ 以上全部
~~~

关键约束（写进 `.asmdef` 的 `references`，编译期即失败，而不是靠约定）：

- **`Presentation` 不依赖 `Pet`。** 表现层通过 `Core.IPetPresenter` 接口被反向驱动，
  所以"行为规则被写进动画脚本"在结构上不可能发生。
- **`AR` 只依赖 `Core`。** 交互层通过 `Core.ITrackingSource` / `IMarkerRegistry` 拿数据，
  因此换掉 Vuforia 或换成模拟源，业务层一行都不用改。
- **`Core` 不依赖任何东西。** 它是共享词汇表：接口、枚举、只读结构。
- 没有任何程序集反向依赖 `App`，所以 `App` 可以随时替换组装方式（例如改成依赖注入容器）。

---

## 3. 程序集与目录对照

| 目录 | 程序集 | 主要文件 | 是否已写 |
| --- | --- | --- | --- |
| `Scripts/Core/` | `ZQXNXS.ARPet.Core` | `TrackingTypes.cs`、`PetState.cs`、`InteractionEvents.cs`、`PresentationContracts.cs`、`ArContracts.cs` | 是 |
| `Scripts/AR/` | `ZQXNXS.ARPet.AR` | `TrackingStateMachine.cs`、`MarkerRegistryBase.cs`、`StubTrackingSource.cs`、`VuforiaTrackingSource.cs` | 是（Vuforia 部分为条件编译） |
| `Scripts/Interaction/` | `ZQXNXS.ARPet.Interaction` | `FeedingRule.cs`、`TouchPokeRule.cs`、`InteractionPipe.cs` | 是 |
| `Scripts/Pet/` | `ZQXNXS.ARPet.Pet` | `PetConfig.cs`、`PetNeeds.cs`、`BehaviorPolicy.cs`、`BehaviorStateMachine.cs` | 是 |
| `Scripts/Presentation/` | `ZQXNXS.ARPet.Presentation` | `PetPresenter.cs` | 是 |
| `Scripts/UI/` | `ZQXNXS.ARPet.UI` | `SafeAreaFitter.cs`（含 `UiInputBlocker`、`TrackingHintText`） | 是 |
| `Scripts/Persistence/` | `ZQXNXS.ARPet.Persistence` | `SaveFile.cs`、`SaveService.cs`、`SaveCoordinator.cs` | 是 |
| `Scripts/Platform/` | `ZQXNXS.ARPet.Platform` | `PlatformServices.cs`（权限 / 生命周期 / 拍照占位） | 是 |
| `Scripts/App/` | `ZQXNXS.ARPet.App` | `AppRoot.cs` | 是 |
| `Scripts/Editor/` | `ZQXNXS.ARPet.EditorTools` | `ProjectSetup.cs` | 是 |
| `Scripts/Tests/EditMode/` | `ZQXNXS.ARPet.Tests.EditMode` | 四份测试（状态机、投喂与数值、跟踪与交互、存档） | 是 |
| `Scenes/` | — | `01_TrackingSmoke`、`02_Main` | **未生成**，需在编辑器中执行菜单生成 |
| `Prefabs/`、`Animations/`、`Configs/`、`Art/UI`、`Art/VFX`、`Audio/` | — | — | 目录已建，内容待做 |
| `../StreamingAssets/`、`../Plugins/Android/` | — | Vuforia 设备数据库、原生插件 | 目录已建，内容待做 |

---

## 4. 类图：核心协作关系

~~~mermaid
classDiagram
    class ITrackingSource {
        <<interface>>
        +bool IsRunning
        +string PauseReason
        +GetSample(MarkerSlot) PoseSample
        +AllMarkers PoseSample[]
        +StartTracking()
        +PauseTracking(string)
    }
    class IMarkerRegistry {
        <<interface>>
        +IsRegistered(MarkerSlot) bool
        +GetTargetSize(MarkerSlot) Vector3
        +IsPoseValid(MarkerSlot, float) bool
        +ToDenLocal(MarkerSlot, Vector3, bool) Vector3
        +TryGetDenPose(out Vector3, out Quaternion) bool
    }
    class IInteractionEventSink {
        <<interface>>
        +TrySubmit(InteractionEvent) bool
    }
    class IPetPresenter {
        <<interface>>
        +PlayBehavior(PetBehaviorId, string, bool, float)
        +SetExpression(PetExpression, float)
        +PlaySfx(string)
        +PlayFx(string)
    }
    class IPetStateProvider {
        <<interface>>
        +Get(PetNeed) float
        +Snapshot PetState
        +Apply(PetNeed, float, float) bool
    }

    class MarkerRegistryBase
    class VuforiaTrackingSource
    class StubTrackingSource
    class TrackingStateMachine
    class FeedingRule
    class TouchPokeRule
    class InteractionPipe
    class BehaviorStateMachine
    class BehaviorPolicy
    class PetNeeds
    class PetConfig
    class PetPresenter
    class SaveCoordinator
    class SaveService
    class AppRoot

    ITrackingSource <|.. VuforiaTrackingSource
    ITrackingSource <|.. StubTrackingSource
    IMarkerRegistry <|.. MarkerRegistryBase
    MarkerRegistryBase <|-- VuforiaTrackingSource
    MarkerRegistryBase <|-- StubTrackingSource
    IInteractionEventSink <|.. AppRoot
    IPetPresenter <|.. PetPresenter
    IPetStateProvider <|.. PetNeeds

    AppRoot --> ITrackingSource
    AppRoot --> IMarkerRegistry
    AppRoot --> BehaviorStateMachine
    AppRoot --> FeedingRule
    AppRoot --> TouchPokeRule
    AppRoot --> InteractionPipe
    AppRoot --> SaveCoordinator
    AppRoot --> TrackingStateMachine

    InteractionPipe --> IInteractionEventSink
    BehaviorStateMachine --> IPetStateProvider
    BehaviorStateMachine --> IPetPresenter
    BehaviorStateMachine --> BehaviorPolicy
    BehaviorStateMachine --> PetConfig
    PetNeeds --> PetConfig
    FeedingRule --> PoseSample
    SaveCoordinator --> PetNeeds
    SaveCoordinator --> SaveService
~~~

---

## 5. 关键设计决策

### 5.1 跟踪层只输出"位姿 + 新鲜度"，不输出"能不能玩"

`PoseSample` 带 `SampleTime`，由 `PoseFreshness` 判断过期（默认 0.35 s）。
**卡片移出镜头后跟踪器会把位置停在上一次结果**，所以决策必须基于"最后一次更新时间"，
而不是"最后一次已知位置"。这是"丢失跟踪就暂停计时与投喂"的实现基础。

### 5.2 交互判定与行为决策分离

- `FeedingRule` 回答：**这算不算一次投喂？**（双目标有效 + 距离 + 高度 + 停留 0.6 s + 4 s 冷却 + 离开后再次进入）
- `BehaviorStateMachine` 回答：**宠物接不接受？**（能不能打断当前行为、动画是否存在）

分开的好处：调"多近才算投喂"不用碰行为逻辑；调"吃饭时能不能被摸"不用碰几何判定。

### 5.3 同一次进入只结算一次，用事件 Id 而不是布尔标志

每次合法进入产生一个自增 `InteractionEvent.Id`，唯一标识为 `(Kind, Source, Id)`。
状态机在每个 `(Kind, Source)` 事件流中记录最大已消费序号，拒绝不大于该序号的重复提交，包括之前已拒绝的事件。
这样既避免跟踪恢复后重放同一事件，也不会让触屏序号阻断食物卡序号。
投喂规则在无效帧清空连续停留，保留结算后的离开锁；只有有效位姿证明卡片移出区域才能重新解锁。

### 5.4 唯一结算点

进食的数值结算只发生在 `BehaviorStateMachine.CompleteCurrent()` 的 `OnExitSettled` 表里，
在行为时长（3.2 s）到达时触发一次。源资产的 `Consume`（2.4 s）是**设计标记**，当前未接入事件。
如果将来改用 Animation Event，必须同时关掉时长结算，二者只能留一个。
AppRoot 先推进上一帧的行为，再提交本帧交互，避免 Eat 提前计时。只有 Idle 会因困倦放慢，Eat 保持 1 倍速。

### 5.5 未制作动画的行为在代码里显式关闭

`PetConfig.Aspect.HasAnimatorClip()` 对 `Petted` / `Resting` / `Chasing` 返回 `false`，
因此 `AllowedBehaviors` 只含 `Idle` 与 `Eating`。
**状态机不会切到一个没有动画的状态，界面也不会显示"可用动作"里其实做不了的动作。**
动画做好后改这一个方法即可解锁。
默认行为同样受白名单限制，不能选单次进食构成自动结算循环；单次行为必须非循环且具有有限正时长。

### 5.6 界面输入不得穿透到角色

`UiInputBlocker.Blocks` → `AppRoot.FeedTouchPointerDown(overUi)` → `TouchPokeRule.PointerDown`：
落在 UI 上的按下会被直接吞掉，抬起时不会产生逗弄事件。

### 5.7 触屏逗弄如实命名

代码里叫 `TouchPokeRule`（触屏戳一下），界面文案与报告里**不得**写成"识别到真实手部接触"。
实现中没有手部检测，也没有手部追踪 SDK。

### 5.8 数值变化与写盘节奏分离

`PetNeeds.Apply` 保留每个可表示的数值变化并置 `Dirty`；`SaveCoordinator.Tick` 满足“数值或时长有变化 + 距上次尝试 ≥5 s”才写盘，失败同样节流。
切后台、退出、拍照等关键节点可强制 `Flush`。异常退出仍可能丢失最近一次成功保存之后的进度。
`SaveService` 刷盘 `.tmp` 后优先原子替换并保留 `.bak`；平台不支持时先刷盘备份再替换。
读档按主档、完整 `.tmp`、`.bak` 恢复，无可用候选时才新建，坏档另存 `.corrupt_*`。

### 5.9 离线衰减有上限

`PetConfig.MaxOfflineSeconds`（默认 12 小时）限制离线补算时长，
避免"一周没开，宠物数值直接见底"。
饥饿随时间增加，开心和精力随时间下降；补算保留 Dirty，下一次保存带上补算结果。恢复状态时保留累计交互次数和累计游玩时长，新档不覆盖配置初值。

### 5.10 模拟跟踪源让业务链路先跑通

`StubTrackingSource` 用两个 Transform 代理两张卡，走的是**和真机完全相同**的判定与状态机代码。
它让 Compile → 状态机 → 界面这条链路能在没有 Vuforia、没有打印机、没有手机的情况下先验证。
**它不代表 AR 跟踪可用**，也不能用来证明识别率、稳定性或性能。

---

## 6. 一次投喂的完整时序

~~~mermaid
sequenceDiagram
    participant U as 用户
    participant T as ITrackingSource
    participant R as FeedingRule
    participant P as InteractionPipe
    participant B as BehaviorStateMachine
    participant N as PetNeeds
    participant V as IPetPresenter
    participant S as SaveCoordinator

    U->>T: 把食物卡放到小窝卡旁
    T-->>R: PoseSample(Den) / PoseSample(Food) 逐帧更新
    R->>R: 双目标有效? 距离≤0.10m? 高度≤0.06m?
    R->>R: 停留累计 0.6s
    R-->>P: InteractionEvent(Id=1, Feed)
    P->>P: AR 可用?
    P->>B: TrySubmit(事件)
    B->>B: Id > 本事件流已消费Id? 可打断?
    B->>N: 应用 OnEnter（开心度 +0.02）
    B->>V: PlayBehavior(Eating, "Eat", loop=false, 0.15s)
    Note over B: 推进 3.2s（Track 期间持续）
    B->>N: 应用 OnExitSettled（饥饿度 −0.45）—— 唯一结算
    B->>V: PlayBehavior(Idle, "Idle", loop=true)
    N-->>S: Dirty = true
    S->>S: 距上次尝试 ≥5s 则落盘
~~~

**跟踪在 3.2 s 中途丢失怎么办**：无效采样立即使 `CanInteract` 与 `InteractionPipe.ArAvailable` 为 false，UI 的 Lost 提示独立去抖，
新的空间交互被丢弃；已在执行的 Eat 由 `AppRoot` 在切后台时 `Interrupt`，
普通丢失则等它在时长处正常结算。恢复后**不会**补发投喂事件。

---

## 7. 已完成 / 待完成

### 已完成的骨架（代码层面）

- 7 层 + 装配层 + 编辑器工具 + EditMode 测试共 **11 个 `.asmdef`**，依赖方向已锁定。
- 跟踪、交互、行为、数据四层的核心逻辑与关键规则测试（重复结算、优先级、阈值边界、存档恢复）。
- 条件编译的 Vuforia 适配器与可直接运行的模拟跟踪源。
- 一键生成配置资产与两个场景的编辑器菜单（`桌上有龙 / 1. 生成默认配置与场景`）。
- 工程级 `.gitignore`（排除 Library / Temp / Logs / Obj / Gradle 导出，保留 `.meta` 与 ProjectSettings）。

### 尚未完成（2026-09-16 复核）

| 待办 | 阻塞原因 |
| --- | --- |
| ~~真实 Unity 编译与 Test Runner~~ | **已完成**：11 个程序集编译通过，83 / 83 EditMode 测试通过 |
| ~~场景实体~~ | **已完成**：两个场景与 `PetBehaviorConfig.asset` 已由 `ProjectSetup` 生成，引用正确 |
| ~~Vuforia 适配器编译验证~~ | **已完成**：真实 Vuforia 11.4.4 编译通过，`Status` / `ObserverBehaviour` 成员名被真实编译器接受（原 `versionDefines` 表达式非法的问题已修） |
| **Animator Controller 与 Animator 组件** | 工程内没有任何 `.controller`，场景里 `PetPresenter.animator` 是空引用，**因此编译通过也不会播放动画** |
| 角色材质重建 | `Art/Characters/Pet01/Materials/` 为空，3 个材质待建 |
| Prefab | 需在编辑器中创建 |
| 触屏输入链路 | `TouchInputBridge` 尚不存在，真机触摸不会产生逗弄事件 |
| Vuforia 运行期行为 | 初始化/恢复时序、Observer 状态实际取值未验证 |
| 标记卡图案与打印尺寸 | 设计未定稿 |
| APK 构建与真机验证 | 需标记卡 + 真机；工具链已就绪 |

---

## 8. 证据与推断边界

- 架构分层与各层约束来自 [开题报告](开题报告_AR萌宠互动系统.md) 4.1 节与 [AGENTS.md](../AGENTS.md)"模块职责与实现约束"，
  本文没有新增业务规则，只把已有约束落成可编译的结构。
- 目录规划对齐 [技术栈与开发环境](技术栈与开发环境.md) 第 4 节。
- 角色尺寸、动画时长、结算时刻来自 [奶蛙最终版资产说明](奶蛙最终版资产说明.md) 与 `asset_manifest.json`。
- **所有阈值（0.10 m / 0.06 m / 0.6 s / 4 s / 0.16 m / 0.35 s）都是设计初值，尚未经真机体验调整。**
- 本地 C# 替身编译与回归结果见根目录审查记录；“代码已完成”不表示 Unity / Vuforia / APK 或真机已经验证。
