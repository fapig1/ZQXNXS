# ARPet 代码审查问题与修复记录

更新日期：2026-09-16。

**本次已处理原审查的 24 项问题，并在循环复核中修复 7 个追加缺陷、完成 1 项编辑器 API 兼容性调整。**

**2026-09-16 首次进入真实 Unity 环境验证**（Unity 6000.0.83f1 + 真实 Vuforia Engine 11.4.4），
发现并修复 **1 个此前所有替身验证都不可能发现的 P1 缺陷**（见第 4 轮之后的"第 6 轮"）。
真实环境已确认：11 个程序集全部编译通过、**83 个 EditMode 测试全部通过（0 失败）**、
Vuforia 真实 SDK 编译通过。**Android 构建与真机验证仍未执行。**

最近一次本地替身运行：**83 个规则与存档测试 + 23 个装配及条件分支替身检查，106 项通过，0 项失败**。
另有真实 Unity 结果：见 [TestResults-EditMode.xml](ARPet/TestResults-EditMode.xml)。

## 1. 范围与证据

- 起点：用户提供的 24 项 review 意见；其中“原 34 项规则测试有 4 项失败”来自该审查结果，并非本轮重新运行修改前版本所得。
- 方法：逐项源码复核、按实际 `.asmdef` 依赖分别编译、针对故障补充回归测试，再复核装配时序和配置边界。
- 验证工具：PowerShell 7.6.5 自带 Roslyn，C# 9；使用仓库内的 Unity / Android / Vuforia API 替身与断言、反射运行器。没有安装或下载外部依赖。
- 本次变更限于代码、配置说明、回归验证工具和文档；冻结的奶蛙源资产与 FBX 未作修改。

证据入口：

- [本地验证脚本](Tools/Validation/run-rule-checks.ps1)
- [验证工具说明与限制](Tools/Validation/README.md)
- [最后一次完整结果](Tools/Validation/latest-results.json)
- [第 3 轮时序修复前：96 通过、2 失败](Tools/Validation/Results/round-3-before-timing-fix.json)
- [第 3 轮时序修复后：98 通过](Tools/Validation/Results/round-3-after-timing-fix.json)
- [第 4 轮配置边界修复前：98 通过、8 失败](Tools/Validation/Results/round-4-before-config-fix.json)

## 2. 审查与修复循环

| 轮次 | 审查重点 | 发现与处理 | 验证结果 |
| --- | --- | --- | --- |
| 1 | 用户给出的 24 项意见 | 修复导入、默认值、跟踪与投喂、动画指令、存档及场景保护 | 补充故障回归用例后进入分程序集检查 |
| 2 | 编辑器与条件编译分支 | 发现 3 处 `MenuItem` 参数写法导致编译错误；改为构造参数，同时更新旧材质导入 API | 11 个无 SDK 项目程序集及 10 个 Android / SDK 条件分支替身编译通过；75 项规则测试通过 |
| 3 | AppRoot 装配与平台回调 | 新增 23 项替身检查，其中 2 项揭示新 Eat 提前消耗上一帧时间 | 修复推进顺序后 98 / 98 通过 |
| 4 | 默认行为、时长、重复启动和持续结算 | 新增 8 个测试用例全部复现配置边界缺陷 | 修复后 106 / 106 通过 |
| 5 | 修复后的依赖、暂停/恢复、结算、读写与文档一致性 | 未发现本次检查范围内待修复的严重问题；整理问题记录和交接项 | 以最近一次 106 项通过结果收敛；未将替身结果计作 Unity 或真机测试 |

## 3. 原审查 24 项逐项处理

“已修复”表示源代码问题已处理；平台和 SDK 项仍受第 6 节的真实环境验证边界约束。

| 编号 | 级别 | 问题 | 修复结果与验证依据 | 状态 |
| --- | --- | --- | --- | --- |
| R01 | P1 | 缺失跨命名空间导入 | 补齐 FeedingRule 的 Pet、AppRoot 的 Platform、跟踪测试的 AR 导入；按程序集分别编译通过 | 已修复 |
| R02 | P1 | Eat 结构体默认全零 | 改为 `EatConfig.Default`；默认配置实测可在 0.6 s 停留后产生事件，冷却及再进入参数有效 | 已修复 |
| R03 | P1 | 模拟场景代理未绑定 | 通过 `SerializedObject` 保存 `denProxy` / `foodProxy`；生成工具的替身检查确认两个引用均赋值 | 已修复，Unity 序列化待验证 |
| R04 | P1 | AR 程序集版本宏字段错误 | 包标识改为 `com.ptc.vuforia.engine`；**当时写的版本条件是 `[0.0.0,)`，该写法在真实 Unity 中被证明非法，见第 6 轮 U01** | 包标识已修复；表达式在第 6 轮再次修正 |
| R05 | P1 | 触屏高序号吞掉后续投喂 | 按 `(Kind, Source)` 分别记录最大消费 Id；被拒绝的触屏事件不再阻断 Feed 1 / 2 | 已修复 |
| R06 | P1 | 小数值增量被永久丢弃 | 保留每个可表示增量，写盘由协调器节流；30 / 60 FPS 推进 60 s 后三项状态均正确累计 | 已修复 |
| R07 | P1 | 遮挡错误解除离开锁 | 无效采样只清空 Dwell，只有有效的远离位置才解锁；遮挡小窝/食物、超冷却后恢复均不重复投喂 | 已修复 |
| R08 | P2 | Android 摄像头权限 API 错误 | 改用 `Permission.HasUserAuthorizedPermission` 和请求回调；拒绝、不再询问、合并请求与焦点恢复路径均有替身检查 | 已修复，Android 授权待实测 |
| R09 | P2 | 延伸跟踪当作可见食物卡 | 新增 `ExtendedTracked`，只有直接 `Tracked` 可结算；Limited / Extended / 未知状态均不能投喂 | 已修复，SDK 状态待实测 |
| R10 | P2 | 暂停后旧位姿被刷新时间戳 | 暂停立即发布 Paused，停止有效采样；迟到的启动回调不能撤销暂停，恢复须等待新采样 | 已修复 |
| R11 | P2 | 丢失去抖计时不断重置 | 从最后有效时刻计时；`CanInteract` 在无效帧立即关闭，UI 提示独立去抖；重复丢失回归通过 | 已修复 |
| R12 | P2 | 小窝丢失时跳过停留清理 | 每帧都调用投喂规则，后台另调用 `SuspendDwell`；AppRoot 装配检查确认恢复后需重新完整停留 | 已修复 |
| R13 | P2 | 新 Feed 重入未完成的 Eating | 删除同目标直接放行；保留原计时和 OnEnter，拒绝事件也参与去重 | 已修复 |
| R14 | P2 | Animator 使用 Eating 而不是 Eat | Core 接口传递配置解析后的动画名；表现层不引用 Pet；Eat、自定义名称和空名称回退用例通过 | 已修复，实际动画待验证 |
| R15 | P2 | 低精力把 Eat 放慢但不延长计时 | 仅 Idle 可放慢，Eat 保持 1 倍速；替身检查验证整个 3.2 s 进食区间 | 已修复，实际 Animator 待验证 |
| R16 | P2 | 开心与精力自然变化方向错误 | 调整为负数，饥饿保持正数，同时更新 JSON 说明；60 s 后预期为 0.51 / 0.51 / 0.79 | 已修复 |
| R17 | P2 | 新档覆盖配置初始值 | `Load(out restoredFromDisk)` 区分新档与有效读档；新用户保留配置初值，AppRoot 自定义初值检查通过 | 已修复 |
| R18 | P2 | 读档遗漏累计交互次数 | 恢复四类计数后再构建状态快照；经过增量变化、再次保存和读取仍保留计数 | 已修复 |
| R19 | P2 | 累计时长从零开始 | 从存档恢复时长，时长变化本身也能触发节流保存；100 s + 新会话 1 s 保存为 101 s | 已修复 |
| R20 | P2 | 先删除旧档导致不可恢复 | 写完整并刷盘后 `File.Replace`，保留 `.bak`；不支持时先刷盘备份再替换；读取可从 `.tmp` / `.bak` 恢复 | 已修复，Android 文件系统待验证 |
| R21 | P2 | 保存失败每帧重试 | 单独记录最后尝试时刻；120 帧 / 2 s 只尝试一次，失败保留 Dirty，达到间隔可重试并恢复成功 | 已修复 |
| R22 | P2 | 两条测试期望与饥饿语义相反 | 进入 Eating 时断言饥饿不变，离线饥饿按正增量累计；未为通过测试反转业务语义 | 已修复 |
| R23 | P2 | 重新生成覆盖手工场景 | 已有场景直接保留；创建缺失场景前使用编辑器保存提示；替身检查确认重跑不改两份已有文件 | 已修复，Unity 场景待验证 |
| R24 | P2 | Editor 程序集没有独立 SDK 宏 | EditorTools 声明同样的包版本宏，避免误报 SDK 缺失；独立分支编译通过 | 已修复，真实包检测待验证 |

## 4. 循环复核发现的追加项

| 编号 | 级别 | 触发条件与问题 | 处理及验证 |
| --- | --- | --- | --- |
| S01 | P1 | 三处 `[MenuItem(..., priority = N)]` 把构造参数写成属性赋值，分程序集编译报 CS0246 | 改为 `MenuItem(..., false, N)`；编辑器程序集替身编译通过 |
| S02 | 兼容性调整 | 编辑器工具仍使用旧的 `ModelImporter.importMaterials` API | 改为 `materialImportMode = ModelImporterMaterialImportMode.None`；替身编译通过，实际 Unity 6 API 仍待核验 |
| S03 | P2 | 本帧刚接受投喂，随后 Tick 又把上一帧时长计入新 Eat | 先推进旧行为再接受本帧交互；修复前 2 项装配检查失败，修复后通过 |
| S04 | P2 | 默认行为可配置为 Eating 或未制作的 Resting，绕过实体投喂与动画白名单 | 默认行为必须为已允许的循环行为，非法默认回落 Idle；无可用默认则拒绝启动 |
| S05 | P2 | Eating 配置为零、负数或无限时长，永远无法完成 | 单次行为必须非循环且具有有限正时长；3 个边界用例通过 |
| S06 | P2 | 重复调用 Start 丢弃执行中的 Eat | Start 改为幂等，保留行为与计时；重复启动回归通过 |
| S07 | P2 | PerSecond 表中的 OnEnter 被每帧重复应用 | 明确分开一次性项与每秒项；20 帧之后不再把精力错误刷满 |
| S08 | P2 | 大时间步把持续效果累计到行为完成之后 | 持续效果最多按剩余行为时长结算；10 s 时间步只应用 3.2 s 的进食持续效果 |

同一路径还补齐了：离线变化保留 Dirty、暂停取消未完成触屏输入、显式删档同时清理恢复档、SDK Observer 停用时拒绝位姿、AR 主场景关闭自动模拟跟踪回退。以上均包含在最终检查范围内。

## 4.5 第 6 轮：真实 Unity 环境首次验证（2026-09-16）

环境：Unity `6000.0.83f1`（Unity 6.0 LTS）+ **真实 Vuforia Engine 11.4.4**（本地 UPM 包
`Packages/com.ptc.vuforia.engine-11.4.4.tgz`，`source: local-tarball`）。
这是本工程第一次脱离替身、在真实编辑器里编译与运行测试。

### 通过项（真实环境实测）

| 项 | 结果 | 证据 |
| --- | --- | --- |
| 11 个程序集编译 | 全部通过，无 `error CS` | `Library/ScriptAssemblies/ZQXNXS.ARPet.*.dll` |
| EditMode 测试 | **83 / 83 通过，0 失败**（NUnit 3.5.0.0） | [TestResults-EditMode.xml](ARPet/TestResults-EditMode.xml)（`result="Passed"`，`failed="0"`） |
| 场景一键生成 | 两个场景 + `PetBehaviorConfig.asset` 生成成功，`AppRoot` 与 `StubTrackingSource` 的 5 个序列化引用全部非零 | `Scenes/01_TrackingSmoke.unity` |
| 存档 JSON | `SaveFile` / `PetState` 只含 `int` / `long` / `float` / `string[]` / 嵌套 `[Serializable] struct`，全部是 `JsonUtility` 支持的类型 | 存档测试在真实 `JsonUtility` 下通过 |

真实环境下测试数与替身环境（83）**完全一致**，说明此前替身对规则层的保真度是够的。

### 本轮发现并修复

| 编号 | 级别 | 问题 | 处理与验证 |
| --- | --- | --- | --- |
| U01 | **P1** | `versionDefines` 的版本表达式写成 `[0.0.0,)`，**Unity 的解析器不接受这种上界为空的方括号范围**，日志抛 `ExpressionNotValidException: '[0.0.0,)' is not a valid expression`。后果：`VUFORIA_ENGINE` **从未被定义**，`VuforiaTrackingSource` 内的真实实现一直处于条件编译关闭状态——即使 Vuforia 包已正确安装也完全无效 | 两个 asmdef 的表达式改为裸版本号 `"0.0.0"`（语义为"该版本或更新"，与 Unity 自带 asmdef 写法一致，如 render-pipelines.core 用 `"1.5.0"` 定义 `HAS_BURST`） |
| U02 | P2（工具缺陷） | 本地校验脚本 `run-rule-checks.ps1` **把 `[0.0.0,)` 硬编码为期望值**，即把 U01 这个 bug 当成了正确标准，因此不可能发现它；且脚本是手动注入 `VUFORIA_ENGINE` 宏的，永远无法验证 Unity 的表达式解析器 | 脚本改为：校验包名与 define 非空，并**主动拒绝上界为空的方括号范围**并给出原因说明；同时明确注释"表达式是否真正生效仍须以真实 Unity 日志为准"。修改后 106 / 106 仍通过 |
| U03 | P2（环境） | `%LOCALAPPDATA%\Unity` 是指向 `D:\Cache_Unity` 的**断链符号链接**（目标目录不存在），导致 **Unity 完全写不出 `Editor.log`**——全盘搜索确认磁盘上没有任何 Editor.log，出错时没有第一手诊断材料 | 创建 `D:\Cache_Unity`；重启后 `Editor.log` 恢复正常写入。已记入 SETUP 常见坑 |

#### U01 的验证证据（修复前 → 修复后）

| 检查点 | 修复前 | 修复后 |
| --- | --- | --- |
| 日志中的 `ExpressionNotValidException` | 反复出现 | **0 处** |
| `ZQXNXS.ARPet.AR.dll` 大小 | 12,288 字节 | **13,312 字节** |
| AR.dll 中的 Vuforia 类型名 | 无 | `VuforiaApplication`、`VuforiaBehaviour`、`ObserverBehaviour`、`get_TargetStatus` |
| `EditorTools.dll` 中"VUFORIA_ENGINE 未定义"警告串 | 存在 | **已被条件编译排除**（0 处） |

**自动化检查的天花板在这里暴露得很清楚**：替身能验证逻辑、能验证依赖方向、能验证条件分支的控制流，
但**不能验证构建系统自身的配置语法**。凡是"配置 Unity 编译器/包管理器"的字符串，
都必须以真实编辑器的日志为准。

### 真实 Vuforia SDK 带来的结论修正

此前文档多处把"Vuforia API 成员名"列为待验证项，措辞是按 Vuforia 11.x 常见写法书写、
仅做过替身编译。**2026-09-16 起该结论需要修正**：`VuforiaTrackingSource` 的真实实现
已对着真实 Vuforia 11.4.4 编译通过，`ObserverBehaviour.TargetStatus.Status` 与 `Status`
枚举成员名被真实编译器接受，**成员名这一层不再属于待验证项**。
仍待验证的是**运行期行为**：初始化与恢复时序、Observer 状态的实际取值、以及真机跟踪表现。

---

## 5. 复现本地检查

在项目根目录使用 PowerShell 7：

```powershell
pwsh -NoProfile -File ./Tools/Validation/run-rule-checks.ps1
```

当前结果摘要：

```text
Offline compilation: 28 assemblies. Rules: 83; stub integration checks: 23. Total: 106 passed, 0 failed.
```

28 个编译输出由 **11 个无 SDK 项目程序集、10 个 Android / SDK 替身分支程序集、7 个验证支持程序集**组成，不表示 Unity 工程有 28 个程序集。工程本身仍为 11 个 `.asmdef`。

规则测试位于 [Tests/EditMode](ARPet/Assets/_Project/Scripts/Tests/EditMode)，可在装好 Unity 后通过 Test Runner 再运行。工具中的装配和平台检查使用替身，不会进入 `Assets`，不会成为应用代码。临时存档与模拟场景文件均位于隔离的测试目录。

## 6. 仍需真实环境验证

| 项目 | 尚未确认的内容 | 后续入口 |
| --- | --- | --- |
| ~~Unity 6 编译与 Test Runner~~ | **已执行（2026-09-16）**：11 个程序集编译通过，83 / 83 EditMode 测试通过 | 结果见第 4.5 节 |
| ~~Vuforia SDK 程序集引用与版本宏~~ | **已执行（2026-09-16）**：11.4.4 本地包解析成功，真实 SDK 编译通过，宏已定义 | 结果见第 4.5 节 U01 |
| Vuforia 运行期行为 | 初始化与恢复时序、Observer 状态实际取值、位姿精度 | SETUP 第 5 节第 9 条 |
| Animator 与角色导入 | **`PetPresenter.animator` 目前是空引用，工程内没有任何 Animator 组件与 Animator Controller**，因此即使编译通过也不会播放动画；另需核对 Idle / Eat 状态名、Generic 骨架与播放时长 | SETUP 第 6 节 |
| 角色材质 | `Art/Characters/Pet01/Materials/` 为空，3 个材质（Skin / Eyes / Oral）尚未重建 | SETUP 第 6 节 |
| 触屏输入链路 | `TouchInputBridge` 尚不存在，真机触摸不会产生逗弄事件 | 见项目待办清单 |
| Android / iQOO Z9 Turbo | 权限拒绝和系统设置恢复、切后台、单/双卡、遮挡、文件系统与实际性能 | 构建后进行真机记录 |

本地文件测试使用真实 Windows 临时文件，但没有实施物理断电或真实进程崩溃测试；恢复用例通过构造中断后的文件布局验证。JSON 在本地工具中使用 `System.Text.Json` 替身，不能替代 Unity 的 `JsonUtility` 实测。

这些属于尚未执行的验证，不应写成“已通过”，也不以替身测试推定 APK 可用。后续真实环境如发现新的严重问题，应在此文件继续追加审查轮次、复现条件与修复证据。
