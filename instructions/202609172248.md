II. Remaining Phase Tasks
P1 Make 02_Main Actually Runnable (Most Urgent Step)
- To do: Place the Pet01 instance into 02_Main (the three-material set + Animator + PetPresenter), parent it under DenCardTarget so it follows the den card, and wire up AppRoot's presenter/anchoredPetRoot references.
- Checkpoint: In the AR main scene, autoCreateStubSource must remain disabled (leaving it on will silently switch to simulation mode when a tracking source is missing, making it only appear to work on a real device); the character's orientation must match the local coordinates of Vuforia's ImageTarget (Blender export uses +Z as forward). Only after this step is completed can you achieve the first "pet visible in AR" milestone.

P2 Integrate the Three Supplementary Animations into Unity
- To do: Set the three FBX files to Generic and duplicate the Avatar; enable Loop Time for DozeLoop/WalkLoop; add the three clips to Pet01Controller; enable the Petted/Resting/Chasing rows in PetConfig.Aspect.HasAnimatorClip.
- Finish WalkLoop first: it is currently the only ground-penetration criterion that has not passed. The direction has been identified (hip lift must lead knee bend), but the fix has not yet been verified.
- Checkpoint: Chasing is a looping animation, but its DurationSeconds in PetBehaviorConfig.asset is 0—how looping behaviors enter, exit, and settle must be clearly defined, otherwise the state machine will end immediately or get stuck.

P3 UI Layer (Currently Empty)
- Current state: UI/ contains only SafeAreaFitter.cs (UiInputBlocker is also in the same file); neither scene has a Canvas or EventSystem.
- To do: Canvas + EventSystem, status panel (hunger/sleepiness/happiness), tracking prompt (consumes TrackingUiState), tablet/phone safe-area adaptation, and input isolation.
- Checkpoint (important architectural gap): The UI assembly only references Core and cannot reference AppRoot (App references UI one-way). AppRoot currently exposes TrackingState/PetSnapshot/CurrentBehavior as public members, but the UI layer cannot access them. Either add a UI-facing read-only interface in Core implemented by AppRoot, or put the panel in the App layer—this is a design issue that must be decided before work begins. The existing IPetStateProvider in Core is for the behavior layer, not for UI.
- Also, before a Canvas exists, TouchInputBridge.IsOverUi() is always false because EventSystem.current == null, so the discipline that "input does not pass through UI" has not actually been verified yet.

P4 Ball-Chasing Gameplay (Contract Only, No Implementation)
- Current state: The InteractionEventKind.Play, PetBehaviorId.Chasing, and PlayAreaRadius contracts all exist, but no rule generates Play events (only FeedingRule and TouchPokeRule exist), and there is no ball in the scene.
- To do: Rules and event generation for the ball/target object, the ball object and its movement, confinement to the virtual plane defined by the den card, and completion of the configuration.
- Checkpoint: PetBehaviorConfig.asset has no gameplay-area radius field, and Chasing has a duration of 0; chasing the ball does not mean a real desktop has been scanned, so the documentation wording must be consistent.

Please choose an appropriate level of progress, complete the tasks, and enable me to perform the most basic testing with my phone.
