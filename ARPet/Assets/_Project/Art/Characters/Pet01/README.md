# Pet01 导入资源（最终版）

> **2026-09-11：用户已确认当前奶蛙三维模型为最终版本，形体、骨骼与两段动画冻结。**
> 详见 [奶蛙最终版资产说明](../../../../../../Docs/奶蛙最终版资产说明.md) 与
> `final_manifest.json`（本目录内的机器可读冻结记录）。

**2026-09-16 更新**：Unity 工程已初始化，模型已导入并设为 **Generic** 骨架（导入参数由菜单
**桌上有龙 / 1. 生成默认配置与场景** 写入，材质导入已关闭）。**尺寸、朝向、26 根骨骼与材质重建尚未核对**，
核对清单见 [奶蛙最终版资产说明](../../../../../../Docs/奶蛙最终版资产说明.md) 第 5 节与 SETUP 第 6 节。
材质目录 `Materials/` 目前为空。

- `Models/Pet01.fbx`：中性站姿、网格与 26 根骨骼。
- `Models/Pet01_Idle.fbx`：相同模型和骨架，3.0 秒待机动画（循环）。
- `Models/Pet01_Eat.fbx`：相同模型和骨架，3.2 秒单次进食动画（不循环）。
- `Textures/Pet01_BaseColor.png`：2048 × 2048 色彩图集。
- `asset_manifest.json`：源资产、尺寸、动作、结算标记和验证状态。
- `final_manifest.json`：最终版冻结记录（哈希、几何、动作、未制作项）。

采用 Generic。场景中只需放置一只主模型，引用两个动作 FBX 中的 clip；不需要实例化三个 FBX。先以完整 take 验证时长、材质和朝向，Eat 不循环、Animator 不应用根位移。

进食的数值结算只发生一次，在相对 clip 起点 2.4 s 的 `Consume` 处（当前由行为层按 3.2 s 时长触发）。
源文件的 `Consume` 帧标记只是设计元数据，不会自动变成 Unity Animation Event。

首次在所选 Unity 6 LTS 中导入后保留生成的 `.meta`。
导入步骤、材质 Smoothness 初值与验证清单见
[奶蛙最终版资产说明](../../../../../../Docs/奶蛙最终版资产说明.md) 第 5 节；
一键设置导入参数可用菜单 **桌上有龙 / 1. 生成默认配置与场景**。

目前没有伪造导入器配置或已测试的 Prefab。**尚未制作**：WalkLoop、TouchReact、Happy、DozeLoop 四类动作，
以及二维表情与音效。
